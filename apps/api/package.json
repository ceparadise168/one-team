{
  "name": "@one-team/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint \"src/**/*.ts\"",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm build && node --test \"dist/**/*.test.js\""
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.923.0",
    "@aws-sdk/client-secrets-manager": "^3.845.0",
    "@aws-sdk/client-sqs": "^3.923.0",
    "@aws-sdk/lib-dynamodb": "^3.923.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.152"
  }
}
